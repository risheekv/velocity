server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ride_fast_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: podarindore

  jpa:
    hibernate:
      ddl-auto: update
    
    show-sql: true
    properties:
      format_sql: true
